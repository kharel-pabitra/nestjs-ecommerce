services: 
  postgres: 
    image: postgres:16
  
    container_name: testing-postgres-image 

    restart: unless-stopped

    environment: 
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: testing-postgres-image

    ports: 
      - "9000:5432"